/*
We can successfully parse expressions like
```c
int j = add_function(i + 1, 3) * -12 + -6;
int k;
int i = 0;
do {
  i++;
} while (i < 3);
```
*/
// Yeah my bad, I accidentally made structs use commas instead of semi-colons
// Will fix in future
struct user {
  int age;
  char *name;
};
